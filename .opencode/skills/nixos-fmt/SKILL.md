---
name: nixos-fmt
description: Format the repo with nixfmt-tree from the flake formatter. Use before every commit.
---

Run `nix fmt`. Defined as `nixfmt-tree` in `flake.nix`. Verify with `nix fmt --check` in review.
